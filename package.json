{
  "name": "reviews-platform-api",
  "version": "1.3.0-loyalty-checkout-beta",
  "description": "Reviews and loyalty platform API with separate loyalty DB and checkout redemption beta foundation",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js && node --check src/app.js && find src public Shopify-Liquid/assets -name '*.js' -print0 | xargs -0 -n1 node --check"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "mongoose": "^8.3.2",
    "nodemailer": "^6.9.16"
  }
}
