# Loyalty checkout redemption beta extension

This is a beta checkout UI extension scaffold. It renders a logged-in customer's available loyalty points and can request a checkout redemption from the app API.

It is intentionally gated behind loyalty checkout settings and should not be enabled for merchants until the Shopify extension is deployed and native discount issuing has been tested.

Backend endpoint family: `/api/loyalty/checkout/*`.
