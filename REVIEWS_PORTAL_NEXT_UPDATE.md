# Reviews Portal Next Update

This full repo-format update adds:

- Permanent Reviews Portal outstanding-send controls.
- Outstanding queue modal for due/failed/waiting-delivery review jobs.
- Manual customer-send retry endpoint with explicit confirmation and delivery override protection.
- Shop-proof sends remain locked to the saved shop sender/support email.
- Email Builder canvas: plus-button modules, draggable sections, 20% width snap and 5% height snap controls.
- Saved review email template sections now preserve module width/height hints.
- All Reviews SEO Liquid page redesign with floating background silhouettes, central search bar, review-linked product recommendations, flavour/profile chip search and richer public API recommendations.

Verified with:

```bash
npm run deploy:preflight
```

Result:

```text
Relative require verification passed.
```
