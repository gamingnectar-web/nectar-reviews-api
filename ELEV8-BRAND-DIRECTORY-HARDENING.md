# ELEV8 Brand Directory hardening

Adds a third direct API mount at `/api/admin/brand-directory`, explicitly forwards the current
Shopify shop domain, and replaces the unhelpful `Not found` message with a real routing diagnostic.
